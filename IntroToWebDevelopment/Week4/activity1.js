// You should see this message appear in the console when you first load the
// page, this line can be uncommented
console.log("hello world");

// Recall the syntax for making functions
function sumNumbers(x, y) {
    return x + y;
}

// TASK:
// Make additional functions to subtract, multiply and divide two numbers

// Uncomment the below code to check that the functions work

// console.log(sumNumbers(10, 11));
// console.log(subtractNumbers(8, 2));
// console.log(multiplyNumbers(5, 4));
// console.log(divideNumbers(20, 2));

// We can pass the output of one function to another, uncomment the line below to see
// console.log(multiplyNumbers(sumNumbers(2, 2), sumNumbers(5, 3)));

// Using the functions you created find the solutions to the following problems:
//
// (7 * 8) - (2 / 4)
//
// (11 * 7) * (1 - 2) + (22 * -8)
//
// ((10 + 20) * (4 / 2) * (7 - 2)
